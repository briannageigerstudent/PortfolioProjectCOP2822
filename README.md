# PortfolioProjectCOP2822
Link to deployed site :
https://briannageigerstudent.github.io/PortfolioProjectCOP2822/

Git hub pages deployment steps :

In terminal, navigate to top level of project folder.

Initialize git (git init)

Add files via git add . 

Commit via git commit -m "Comment here"

git push to push to git hub

From there, git hub pages allows you to deploy in accordance to github repo files.
